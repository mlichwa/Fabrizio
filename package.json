{
  "name": "fabrizio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "reset": "rimraf dist && rimraf .cache",
    "start": "rimraf dist && parcel serve src/index.html",
    "copy": "cp static/fabrizio_animation.json dist/",
    "build": "rimraf dist && parcel build src/index.html --no-cache --public-url ./"
  },
  "author": "Michal Lichwa",
  "license": "",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "autoprefixer": "^10.4.16",
    "parcel": "^2.11.0",
    "postcss-modules": "^6.0.0",
    "rimraf": "^5.0.5",
    "sass": "^1.69.7"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.1"
  }
}
